import { t as supabase } from "./client-Ca5S0WhP.js";
import { jsx, jsxs } from "react/jsx-runtime";
import { useTranslation } from "react-i18next";
//#region src/lib/languages.ts
var LANGUAGES = [
	{
		code: "ko",
		name: "한국어",
		flag: "🇰🇷"
	},
	{
		code: "en",
		name: "English",
		flag: "🇺🇸"
	},
	{
		code: "ja",
		name: "日本語",
		flag: "🇯🇵"
	},
	{
		code: "zh",
		name: "中文",
		flag: "🇨🇳"
	},
	{
		code: "es",
		name: "Español",
		flag: "🇪🇸"
	},
	{
		code: "fr",
		name: "Français",
		flag: "🇫🇷"
	},
	{
		code: "de",
		name: "Deutsch",
		flag: "🇩🇪"
	},
	{
		code: "ru",
		name: "Русский",
		flag: "🇷🇺"
	},
	{
		code: "pt",
		name: "Português",
		flag: "🇧🇷"
	},
	{
		code: "vi",
		name: "Tiếng Việt",
		flag: "🇻🇳"
	}
];
function langName(code) {
	return LANGUAGES.find((l) => l.code === code)?.name ?? code.toUpperCase();
}
//#endregion
//#region src/components/LanguageSelect.tsx
function LanguageSelect({ persist = false }) {
	const { i18n } = useTranslation();
	return /* @__PURE__ */ jsx("select", {
		value: i18n.language?.slice(0, 2) ?? "en",
		onChange: async (e) => {
			const lang = e.target.value;
			await i18n.changeLanguage(lang);
			if (persist) {
				const { data } = await supabase.auth.getUser();
				if (data.user) await supabase.from("profiles").update({ preferred_lang: lang }).eq("id", data.user.id);
			}
		},
		className: "bg-surface-2 border border-border rounded-md px-2 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring",
		children: LANGUAGES.map((l) => /* @__PURE__ */ jsxs("option", {
			value: l.code,
			children: [
				l.flag,
				" ",
				l.name
			]
		}, l.code))
	});
}
//#endregion
export { LANGUAGES as n, langName as r, LanguageSelect as t };
